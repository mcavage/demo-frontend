box: node
build:
  steps:
    - npm-install

deploy:
  steps:
    - internal/docker-push:
        username: $DOCKER_USERNAME
        password: $DOCKER_PASSWORD
        repository: mcavage/demo-frontend
        ports: 8080
        cmd: /bin/bash -c "cd /pipeline/source && npm start"
